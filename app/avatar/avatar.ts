namespace $ {

	/**
	 * Shown wherever a user has no avatar of their own. The file name is part of the
	 * spec: the e2e suite checks that the `src` of every avatar points at it.
	 */
	export const $realworld_app_avatar = 'realworld/app/assets/default-avatar.svg'

}
