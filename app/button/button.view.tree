- A real `button` element: $mol_button renders a custom one, and both the shared
- stylesheet and the test contract address buttons by tag.
$realworld_app_button $mol_button
	dom_name \button
	attr *
		^
		class <= class_names \btn
		type <= type \button
